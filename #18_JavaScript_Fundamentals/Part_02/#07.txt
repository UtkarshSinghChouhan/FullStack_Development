// Define the radius of the circle
const radius = 10;

// Calculate the area of the circle
const area = Math.PI * Math.pow(radius, 2);

// Display the result
console.log(`The area of the circle with a radius of ${radius} is approximately ${area.toFixed(2)}`);